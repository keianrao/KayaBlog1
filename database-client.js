
databaseURL = "localhost:8080"

function connectToDatabase()
{
    // We should return a Promise here.
    // Also, start doing the backend please. We need to be testing things as we go.

    return Promise.resolve(true);
}
